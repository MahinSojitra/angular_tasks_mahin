import { IfNoNotesDirective } from './if-no-notes.directive';

describe('IfNoNotesDirective', () => {
  it('should create an instance', () => {
    const directive = new IfNoNotesDirective();
    expect(directive).toBeTruthy();
  });
});
